---
title: "Expressions Ensemble: When Benchmark Accuracy Doesn't Mean What You Think"
subtitle: "How weak supervision ensemble methods with stock photos outperformed curated datasets on real-world emotion recognition"
date: "2026-02-11"
categories: [computer-vision, weak-supervision, validation, applied-research]
image: "exe-hero.png"
toc: true
toc-depth: 2
---

![ExE Movie Emotion Distribution vs Benchmarks](/assets/images/all_diff_chart.png)

## The Problem Nobody Talks About

Emotion recognition is one of those fields where leaderboard performance and real-world utility have quietly diverged. Models trained on FER2013 or RAF-DB post impressive test accuracy — 71% and 90% respectively in my tests — and then produce completely uninterpretable results when you point them at actual video content.

I wanted to understand *why*, and whether the problem was fixable without massive labeled datasets.

The short answer: it's a data problem, not a modeling problem. And the fix turned out to be surprisingly simple.

## The Setup

The standard approach to emotion recognition looks like this: take a curated dataset (FER2013 has ~35,000 labeled images; RAF-DB has ~30,000), train a classifier, evaluate on held-out test data, report accuracy. Higher accuracy = better model. Paper published.

The problem is what happens next. FER2013 images are 48x48 grayscale crops from Google image searches, many ambiguous or mislabeled. RAF-DB uses crowd-sourced labels on "in-the-wild" photos that skew heavily toward selfie-style compositions. Neither dataset looks anything like the faces you encounter in movies, surveillance footage, video calls, or any other downstream application.

I hypothesized that **domain alignment between training data and target application matters more than dataset size or benchmark accuracy**. If your training data looks like your target domain, a smaller, noisier dataset should outperform a larger, cleaner one that comes from a different visual distribution.

## The Approach: Weak Supervision via Stock Photos

Rather than manually labeling thousands of images, I built training datasets using keyword searches on stock photo platforms (Pexels and Pixabay). The logic: stock photos of emotions are *performed expressions in context* — actors expressing happiness, sadness, anger in staged but naturalistic scenes. That's much closer to movie performances than lab-posed expressions or selfies.

### Data Collection Design

The critical design decision was **multi-keyword searches combined for depth and breadth**. Instead of searching only for "happy face," I combined related terms: `happy + smiling + joyful`. This approach:

- Casts a wider net across the visual distribution of each emotion
- Reduces platform-specific search algorithm bias
- Produces more varied lighting, angles, and demographics

Then, instead of combining the data which might muddle signal, I trained 4 models and used ensemble classification in my analysis. 

### What I Kept and What I Cut

The initial pull included seven emotion categories. I dropped `disgust` and `neutral` early. Disgust had too few quality results across both platforms. Neutral was a conceptual problem — "neutral face" in stock photography returns posed blankness that doesn't match how neutral actually presents in video (people talking, listening, thinking). Trying to force a category that doesn't have a clean visual prototype creates noise that propagates through training.

Final datasets ranged from roughly 1,500 to 5,000 images across five classes (angry, fear, happy, sad, surprise) with a total of just over 10,000 images. The datasets had real class imbalance. I chose not to aggressively balance this. In real video content, emotions are not uniformly distributed. A model that's seen more examples of common expressions isn't necessarily worse for it.

| Name   |   Total |   Fear |  Surprise |   Sad |   Happy |  Angry |
|:------------------|-----------------------:|--------------------------:|------------------------------:|-------------------------:|---------------------------:|---------------------------:|
| pixabay-v1  |                   1553 |                       189 |                            90 |                      363 |                        451 |                        460 |
| pixabay-v2  |                   2132 |                       233 |                           203 |                      542 |                        988 |                        166 |
| pexels-v1   |                   2242 |                       400 |                           454 |                      513 |                        437 |                        438 |
| pexels-v2   |                   4517 |                       887 |                           863 |                     1047 |                       1058 |                        662 |



## Training: Nothing Exotic

The modeling itself was intentionally straightforward. ResNet18 pretrained on ImageNet, fine-tuned final layers, standard augmentation (flips, rotations, color jitter). MediaPipe for face detection. PyTorch with MLflow for experiment tracking.

I want to be explicit about this because it matters for the argument: **the model architecture contributed nothing novel**. Same backbone, same training procedure, same hyperparameter ranges as the FER2013 and RAF-DB baselines I trained for comparison. The only variable that changed was the data.

This is the whole point. When people reach for architectural complexity to solve performance problems, they're often compensating for a data problem that no amount of attention heads or loss function engineering will fix.

## Validation: Where It Gets Interesting

Test accuracy on held-out data told the predictable story. RAF-DB's curated labels produced 90% accuracy. FER managed just over 70%. The 4 models for ExE ranged from ~50-70%.

If you stopped here, RAF-DB wins.

**I didn't stop here.**

### Movie Timeline Analysis

I ran all three models against approximately 60 full-length films, extracting predictions every 100th frame. The idea was simple: a good emotion recognition model should produce temporal patterns that reflect narrative structure. Comedies should show more happiness. Dramas should show more sadness. Emotional inflection points — reunions, conflicts, revelations — should produce visible shifts.

This is what I mean by **ecological validation**: testing whether model outputs are *interpretable and useful* in a realistic downstream task, not just whether they match held-out labels from the same distribution.

### What the Models Actually Produced

**ExE** generated emotion timelines with visible narrative structure. Across comedies, dramas, and action films, the model's predictions varied by genre and tracked recognizable plot points — reunions, conflicts, resolutions.

**RAF-DB** collapsed into mostly surprise and sad. Despite 90% test accuracy, it predicted "surprise" as the dominant emotion in roughly half of all movies. Timelines were flat — linear accumulation of surprise with minimal variation across genres or narrative beats.

**FER2013** showed similar bais to RAF-DB but tending towards anger and fear.

### Why This Happened

The failure mode is domain mismatch. RAF-DB's training data is dominated by selfie-style close-ups with exaggerated expressions. When the model encounters movie frames — medium shots, varied lighting, subtle expressions, faces in profile — it maps most of them to "surprise," likely because the open-mouth, wide-eye features of surprise are the closest match to its training distribution. FER2013's tiny grayscale crops are even further from movie frames and domain transfer issues are evident in the results.

ExE's stock photo training data includes more varied compositions, lighting conditions, and expression intensities. The domain gap to movie content is smaller, so the model's predictions retain meaningful variation.

## See It For Yourself

I can describe these differences, but the comparison is more convincing when you look at it directly. The [interactive results dashboard](https://expressions-ensemble.streamlit.app/) lets you explore predictions across all 50+ films, compare models side-by-side, and draw your own conclusions. What follows are two examples that illustrate the pattern.

### Genre Comparison

If an emotion model works, comedies should look different from dramas. 

#### Comedies

Here's the emotion distribution across ten comedies — from *Airplane!* to *Scott Pilgram* — for all three models:

::: {.grid}

::: {.g-col-12 .g-col-md-6}
![ExE: Comedy emotion distributions](/assets/images/ExE Comedies.png)
:::
::: {.g-col-12 .g-col-md-6}
![RAF-DB: Comedy emotion distributions](/assets/images/RAF-DB Comedies.png)
:::
::: {.g-col-12 .g-col-md-6}
![FER: Comedy emotion distributions](/assets/images/FER Comedies_stack_emo_by_movie.png)
:::
:::

ExE and RAF detect substantial happy (yellow) and sad (blue), with meaningful variation between films. *Dark Shadows* and *Dodgeball* have different emotional profiles, which tracks — they're very different movies. Of note, RAF-DB shows very strong levels of surprise (purple) for every film regardless of content. FER detects mostly fear and anger with very little happy.

#### Crime

Here's the emotion distribution across seven comedies — from *Black Mass* to *Seven Psychopaths* — for all three models:

::: {.grid}

::: {.g-col-12 .g-col-md-6}
![ExE: Crime emotion distributions](/assets/images/ExE_Crime_stack_emo_by_movie.png)
:::
::: {.g-col-12 .g-col-md-6}
![RAF-DB: Crime emotion distributions](/assets/images/RAF-DB_Crime_stack_emo_by_movie.png)
:::
::: {.g-col-12 .g-col-md-6}
![FER: Crime emotion distributions](/assets/images/FER_Crime_stack_emo_by_movie.png)
:::
:::

ExE and FER detect substantial fear (grey) and anger (red), with ExE also picking up on some surprise (purple). RAF-DB shows very strong levels of surprise (purple) and happy (yellow) for every film regardless of content but almost no fear. FER detects almost entirely fear and anger with very little variation.

### Timeline Narratives

In addition to aggregate statistics, movies are an ideal way to assess narrative arcs and storyline changes in context. 

#### The Hitchhiker's Guide to the Galaxy 

A fun, space adventure with a mix of humor and action. 

::: {.grid}

::: {.g-col-12 .g-col-md-6}
![ExE: Timeline](/assets/images/ExE_timeplot_hitchhikersguidetothegalaxy.png)
:::
::: {.g-col-12 .g-col-md-6}
![RAF-DB: Timeline](/assets/images/RAF-DB_timeplot_hitchhikersguidetothegalaxy.png)
:::
::: {.g-col-12 .g-col-md-6}
![FER: Timeline](/assets/images/FER_timeplot_hitchhikersguidetothegalaxy.png)
:::
:::

ExE picks up on all five emotion categories while RAF never detects fear and FER detects almost nothing but fear. ExE also demonstrates more velocity in emotion detection (more rapid increases over a short period).

#### Big Hero 6 

A super hero animated adventure. Animation should increase the domain gap between training data and the film. 

::: {.grid}

::: {.g-col-12 .g-col-md-6}
![ExE: Timeline](/assets/images/ExE_timeplot_big_hero_6.png)
:::
::: {.g-col-12 .g-col-md-6}
![RAF-DB: Timeline](/assets/images/RAF-DB_timeplot_big_hero_6.png)
:::
::: {.g-col-12 .g-col-md-6}
![FER: Timeline](/assets/images/FER_timeplot_big_hero_6.png)
:::
:::

Again ExE picks up on all five emotion categories with rapid changes likely reflecting narrative changes. RAF collapses mostly onto surprise and FER to fear with slow, linear gains.

These aren't cherry-picked gotchas. The pattern is consistent. Explore the full set on the [dashboard](https://expressions-ensemble.streamlit.app/) and decide for yourself whether benchmark accuracy tells the whole story.

## What I Got Wrong

This project has real limitations and I want to be upfront about them.

**Sad bias.** ExE predicts sad as the dominant emotion in the majority of films. My guess here is that open mouth/talking/neutral faces get lumped into these categories, but more testing is needed including better keyword curation or post-hoc calibration.

**No ground truth.** My validation relies on common-sense genre and plot knowledge. Reasonable people could disagree about the emotional profiles and shifts.

## What This Actually Demonstrates

ExE is not a production emotion recognition system. It's a proof of concept for a methodology:

**Automated dataset curation via keyword-based weak supervision can produce models that outperform benchmark-trained alternatives on ecologically valid tasks.** The approach is reproducible, transferable (same method could apply to gesture recognition, activity classification, or any domain where stock photos exist), and resource-efficient (~10,000 images, single GPU, no manual labeling).

For practitioners, the actionable takeaway is this: before investing in larger datasets or more complex architectures, examine the domain alignment between your training data and your actual application. A smaller dataset from the right distribution will often beat a massive dataset from the wrong one.

## Technical Details

The full codebase, training configs, and data collection scripts are available on [GitHub](https://github.com/pixel-process-dev/expressions-ensemble). The repository includes everything needed to reproduce the pipeline end-to-end.

An [interactive results dashboard](https://expressions-ensemble.streamlit.app/) lets you explore the movie-level predictions and model comparisons directly.

For questions about the methodology or potential applications of this approach, reach out via [Dexterous Data](https://dexterousdata.com).
